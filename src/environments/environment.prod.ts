export const environment = {
  production: true,
  // Este valor será usado apenas como fallback
  // Em runtime, usamos window.location.origin via getAppUrl()
  appUrl: 'd1z9kx11cv7jnv.cloudfront.net', // Será substituído automaticamente em runtime
  cognitoLogoutUrl: 'https://us-east-174rxy2but.auth.us-east-1.amazoncognito.com/logout'
};

